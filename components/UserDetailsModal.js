import { StyleSheet, Text, View } from "react-native";
import React from "react";

const UserDetailsModal = () => {
  return (
    <View>
      <Text>UserDetailsModal</Text>
    </View>
  );
};

export default UserDetailsModal;

const styles = StyleSheet.create({});
